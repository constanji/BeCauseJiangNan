<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>cn.hexinfo</groupId>
        <artifactId>dat-servers</artifactId>
        <version>${revision}</version>
    </parent>

    <artifactId>dat-server-openapi</artifactId>
    <name>DAT : Servers : OpenAPI</name>

    <properties>
        <!--
            覆盖 Spring Boot 3.5.5 BOM 默认的 commons-io 2.15.0。
            Apache POI 5.4.0 的 IOUtils.peekFirstNBytes 调用 BoundedInputStream.builder(),
            该静态方法在 commons-io 2.16.0 才引入。2.15 时运行时抛 NoSuchMethodError。
            注意:本项目没用 spring-boot-starter-parent,所以这个 property 不会被 BOM 解析,
            必须在 <dependencies> 里显式声明 commons-io 才会生效(Maven 最近优先覆盖传递依赖)。
        -->
        <commons-io.version>2.16.1</commons-io.version>
    </properties>

    <dependencies>
        <!-- 显式声明 commons-io 2.16.1,强制覆盖 spring-boot-starter-web 传递来的 2.15.0。
             Apache POI XSSFWorkbook 解析 Excel 时需要 BoundedInputStream.builder() (commons-io 2.16+)。 -->
        <dependency>
            <groupId>commons-io</groupId>
            <artifactId>commons-io</artifactId>
            <version>${commons-io.version}</version>
        </dependency>

        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>${lombok.version}</version>
            <optional>true</optional>
        </dependency>

        <dependency>
            <groupId>cn.hexinfo</groupId>
            <artifactId>dat-sdk</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- Spring Boot Web Starter - 提供Web服务基础功能 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
            <version>3.5.5</version>
            <exclusions>
                <!-- 排除spring-jcl，避免与commons-logging冲突 -->
                <exclusion>
                    <groupId>org.springframework</groupId>
                    <artifactId>spring-jcl</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <!-- SpringDoc OpenAPI UI - 自动生成OpenAPI文档和Swagger UI -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>2.8.11</version>
        </dependency>

        <dependency>
            <groupId>cn.hexinfo</groupId>
            <artifactId>dat-datasource</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- Database Adapters -->
        <dependency>
            <groupId>cn.hexinfo</groupId>
            <artifactId>dat-adapter-mysql</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>cn.hexinfo</groupId>
            <artifactId>dat-adapter-postgresql</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>cn.hexinfo</groupId>
            <artifactId>dat-adapter-duckdb</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- Index/Org 持久化与 Mongo 数据访问 -->
        <dependency>
            <groupId>cn.hexinfo</groupId>
            <artifactId>dat-datastore</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <version>3.5.5</version>
            <scope>test</scope>
            <exclusions>
                <exclusion>
                    <groupId>org.springframework</groupId>
                    <artifactId>spring-jcl</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

    </dependencies>
    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <source>17</source>
                    <target>17</target>
                </configuration>
            </plugin>
            <!-- 父 pom 默认 skip=true，本模块需要跑单测,显式开 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <skip>false</skip>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
