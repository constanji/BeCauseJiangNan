<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>cn.hexinfo</groupId>
        <artifactId>dat-project</artifactId>
        <version>${revision}</version>
    </parent>

    <artifactId>dat-migration</artifactId>
    <name>DAT : Project : Migration</name>
    <description>DAT Migration - Tools for migrating from file-based config to MongoDB</description>

    <dependencies>
        <!-- Lombok -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>${lombok.version}</version>
            <optional>true</optional>
        </dependency>

        <!-- DAT SDK (includes core functionality and ProjectUtil) -->
        <dependency>
            <groupId>cn.hexinfo</groupId>
            <artifactId>dat-sdk</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- DAT Datastore -->
        <dependency>
            <groupId>cn.hexinfo</groupId>
            <artifactId>dat-datastore</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- DAT Datasource -->
        <dependency>
            <groupId>cn.hexinfo</groupId>
            <artifactId>dat-datasource</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- Test Dependencies -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <source>17</source>
                    <target>17</target>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>
